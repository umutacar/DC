# IMPORTANT: 
the module pervasives/ must match that inside the diderot
project plus it should contain diderot/mlx/syntax.py as mlx_syntax.py.  

This is how the two communicate.
  


# Create a standalone executable
pip install pyinstaller
pyinstaller --onefile --windowed file.py
